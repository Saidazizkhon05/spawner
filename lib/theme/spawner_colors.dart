import 'dart:ui';

class SpawnerColors {
  SpawnerColors._();

  // Backgrounds
  static const background = Color(0xFF0A0A14);
  static const surface = Color(0xFF14142B);
  static const surfaceLight = Color(0xFF1E1E3A);
  static const surfaceBorder = Color(0xFF2A2A4A);

  // Primary - electric violet
  static const primary = Color(0xFF7C3AED);
  static const primaryLight = Color(0xFF9F67FF);
  static const primaryDim = Color(0xFF5B21B6);
  static const primaryGlow = Color(0x407C3AED);

  // Accent - cyan
  static const accent = Color(0xFF06B6D4);
  static const accentLight = Color(0xFF22D3EE);
  static const accentGlow = Color(0x3006B6D4);

  // Launch - emerald
  static const launch = Color(0xFF10B981);
  static const launchLight = Color(0xFF34D399);
  static const launchGlow = Color(0x3010B981);

  // Text
  static const textPrimary = Color(0xFFE2E8F0);
  static const textSecondary = Color(0xFF94A3B8);
  static const textMuted = Color(0xFF64748B);

  // Danger
  static const danger = Color(0xFFEF4444);
  static const dangerGlow = Color(0x30EF4444);

  // Card gradient stops
  static const cardGradientStart = Color(0xFF1A1A35);
  static const cardGradientEnd = Color(0xFF12122A);
}
